for n in range(1,1000):
    st = bin(n)[2:]
    if n % 2 == 0:
        st = st + "10"
    else:
        st = st + "11"
    if st.count("1") % 2 == 0:
        st = st + st[-1]
    else:
        st = st + st[-2]
    rez = int(st,2)
    if rez > 44:
        print(n)
    
        
