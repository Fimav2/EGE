for n in range(1,1000):
    st = bin(n)[2:]
    st = st + st[-1]
    if st.count("1") % 2 == 0:
        st = st+ "1"
    else:
        st = st + "0"
    if st.count("1") % 2 != 0:
        st = st + "1"
    else:
        st = st + "0"
    rez = int(st,2)
    if rez > 105:
        print(rez)
        

